<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Visitor</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css">
    <style>
        #banner {
          display: block;
          margin-left: auto;
          margin-right: auto;
          width: 18%;
        }
        body {
            background-color: #702c4c;
        }
      </style>
</head>
<body>
    <img id="banner" src="{{ url_for('static', filename='banner.png') }}" />
    <div class="container py-5">
        <div class="row d-flex justify-content-center align-items-center h-100">
          <div class="col-12 col-md-8 col-lg-6 col-xl-5">
            <div class="card shadow-2-strong"" style="border-radius: 1rem">
              <div class="card-body p-5 text-center">
        <h2>Edit Visitor Information</h2>
        <form method="post" action="{{ url_for('edit_visitor', visitor_id=visitor.id) }}">
            <div class="mb-3">
                <label for="name" class="form-label">Name:</label>
                <input type="text" class="form-control" id="name" name="name" value="{{ visitor.name }}" required>
            </div>

            <div class="mb-3">
                <label for="date" class="form-label">Date of Visit:</label>
                <input type="date" class="form-control" id="date" name="date" value="{{ visitor.date }}">
            </div>
            <div class="mb-3">
                <label for="phone" class="form-label">Phone:</label>
                <input type="tel" class="form-control" id="phone" name="phone" value="{{ visitor.phone }}">
            </div>

            <div class="mb-3">
                <label for="email" class="form-label">Email:</label>
                <input type="email" class="form-control" id="email" name="email" value="{{ visitor.email }}">
            </div>

            <div class="mb-3">
                <label for="resident_last_name" class="form-label">Resident's Last Name:</label>
                <input type="text" class="form-control" id="resident_last_name" name="resident_last_name" value="{{ visitor.resident_last_name }}">
            </div>

            <div class="mb-3">
                <label for="resident_room_number" class="form-label">Resident's Room Number:</label>
                <input type="text" class="form-control" id="resident_room_number" name="resident_room_number" value="{{ visitor.resident_room_number }}">
            </div>

            <div class="mb-3">
                <label for="checkin_time" class="form-label">Check-In Time:</label>
                <input type="text" class="form-control" id="checkin_time" name="checkin_time" value="{{ visitor.checkin_time }}">
            </div>

            <div class="mb-3">
                <label for="checkout_time" class="form-label">Check-Out Time:</label>
                <input type="text" class="form-control" id="checkout_time" name="checkout_time" value="{{ visitor.checkout_time }}">
            </div>
            <button type="submit" class="btn btn-primary btn-lg btn-block btn-success">Update</button>
        </form>
        <hr class="my-4" />
        <a
          href="{{url_for('visitors')}}"
          type="submit"
          class="btn btn-primary btn-lg btn-block btn-danger"
          >Go Back</a
        >
    </div>
</div>
</div>
</div>
</div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>   
</body>
</html>

